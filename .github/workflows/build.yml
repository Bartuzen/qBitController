name: Build

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build project
        run: ./gradlew assembleFreeRelease

      - name: Rename & move APK file
        run: mv composeApp/build/outputs/apk/free/release/composeApp-free-release-unsigned.apk qBitController-unsigned.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: android
          path: qBitController-unsigned.apk

  build-ios:
    runs-on: macos-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Generate Info.plist
        run: ./gradlew generateInfoPlist

      - name: Setup latest Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Build project
        working-directory: iosApp
        run: xcodebuild -scheme iosApp -configuration Release -sdk iphoneos CODE_SIGNING_ALLOWED=NO -derivedDataPath ./build build

      - name: Generate IPA
        working-directory: iosApp
        run: |
          mkdir -p Payload
          mv ./build/Build/Products/Release-iphoneos/qBitController.app Payload/
          zip -r qBitController-unsigned.ipa Payload

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios
          path: iosApp/qBitController-unsigned.ipa

  build-windows:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifactName: windows-x86_64

          - os: windows-11-arm
            artifactName: windows-arm64

    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build project
        run: ./gradlew packageReleaseMsi

      - name: Rename & move artifact
        shell: bash
        run: mv composeApp/build/compose/binaries/main-release/msi/qBitController-*.msi qBitController.msi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifactName }}
          path: qBitController.msi

  build-ubuntu:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifactName: linux-x86_64

          - os: ubuntu-24.04-arm
            artifactName: linux-arm64

    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build project
        run: ./gradlew packageReleaseDeb

      - name: Rename & move artifact
        run: mv composeApp/build/compose/binaries/main-release/deb/qbitcontroller_*.deb qBitController.deb

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifactName }}
          path: qBitController.deb

  build-macos:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            artifactName: macos-arm64

          - os: macos-13
            artifactName: macos-x86_64

    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build project
        run: ./gradlew packageReleaseDmg

      - name: Rename & move artifact
        run: mv composeApp/build/compose/binaries/main-release/dmg/qBitController-*.dmg qBitController.dmg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifactName }}
          path: qBitController.dmg
